# Leave unset to use the Vite dev proxy (see vite.config.js) which forwards
# /api -> http://127.0.0.1:8000 during local development.
#
# For a production build, point this at your deployed FastAPI backend, e.g:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
